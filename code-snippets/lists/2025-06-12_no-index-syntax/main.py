def contains_leather_scraps(items):
    found = False

    # don't touch above this line

    for item in items: # Iterates over each item in items
         if item == "Leather Scraps" : 
            found = True # If leather scraps are found set variable to true
             
    # don't touch below this line

    return found
